# Web app core dependencies (installed automatically by uv sync)
fastapi
uvicorn[standard]
python-dotenv
ebooklib
beautifulsoup4
httpx
lxml
python-multipart

# ── Local inference (Apple Silicon) ──────────────────────────────────────────
# Use uv for full dependency resolution (spacy, misaki, etc. are pulled in):
#
#   uv sync --extra local
#
# Then run the server with:
#   uv run uvicorn main:app --reload
#
# If you prefer plain pip, install manually:
#   pip install git+https://github.com/Blaizzy/mlx-audio.git \
#               soundfile tiktoken "misaki[en]" num2words spacy
