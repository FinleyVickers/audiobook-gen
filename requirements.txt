fastapi
uvicorn[standard]
python-dotenv
ebooklib
beautifulsoup4
httpx
lxml
python-multipart
# Optional: local inference on Apple Silicon (requires macOS + MLX)
# Voxtral support is not yet in a PyPI release — install from GitHub:
# pip install git+https://github.com/Blaizzy/mlx-audio.git soundfile tiktoken misaki num2words
